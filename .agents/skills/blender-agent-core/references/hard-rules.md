# Project operational rules

These supplement the existing loop; API knowledge remains in `knowledge/`. Read [workflow](../../../../docs/blender-ai-workflow.md) for intent and [recipes](recipes.md) for task routing. `MANUAL` means controller discipline, not an implemented hook or gate. Early-table times are UTC on 2026-09-05; later incidents carry their explicit source/time. Detailed session evidence is retained in the source workspace and is not included here.

| Trigger | Required behavior | Enforced by | Source |
|---|---|---|---|
| Every new pass/session | Read active scene/file/version/units and declared controller; include imports, source path and assertions in that call. Do not assume MCP Python locals persist. | ENFORCED (2026-09-06): `agent_runtime.run_file` owns namespace/`__file__`/traceback; `load_lib` re-loads helpers by sha | 08:10:09 missing `framing`; 10:00:21 missing `__file__` |
| Any tool reply | Transport success or `AGENT_OK` text is insufficient; inspect structured status, traceback and declared postconditions. Save complete evidence outside truncated display text. | ENFORCED (E1 done): last-line `AGENT_OK`/`AGENT_FAIL` sentinel decides; `headless-run.sh` exit 0/1/2/3; socket client checks status. E3 partial: `production-gate.py` reports bind scene+spec sha256 | Failure-bearing outputs at 08:24:57, 10:15:59; local exit probe |
| Timeout after mutation was sent | Mark outcome unknown; read/reconcile state before another mutation. Never infer rollback or blindly resend. | MANUAL; E1 | Partial execution failures in session; socket/addon static audit, no lost-reply incident claimed |
| Multiple agents | One writer per GUI/scene; reviewers read-only; batch workers use immutable inputs and disjoint outputs. Review image transformations write to a separate review directory, never source/delivery paths; the controller rechecks delivery hashes after review. | MANUAL; lock implementation deferred | Architect/Security debate, no concurrent-writer incident claimed |
| New functional/reference requirement | Update current contract and dependent gates; resolve payload/duty/packaging before committing mechanical design. Keep accepted requirements across presentation changes. | MANUAL; E5 | 09:34:32 engineering correction; 09:36:32 payload/duty; 09:43:08 housing growth |
| Reference construction | Blockout and camera-matched comparison before details; state unseen geometry as inference. | Existing image-to-3d router + MANUAL visual review | 08:37:45 form polish; 09:10:16 reference fidelity correction |
| Mechanical/reference reconstruction | Primary view governs visible count, facing, placement and silhouette; close-ups govern local form. Read applicable drawings/specifications before deciding routing, stations, taps, or connection anatomy; distinguish fact, design choice and unknown. | MANUAL; component workflow | 2026-09-09 instrument station correction |
| New geometry family | Controller proves the native identity-risk prototype before delegated reuse. Constructor promotion needs declared axes/ports/range plus a second real consumer and regression proof. | MANUAL; component workflow | 2026-09-09 reusable instruments |
| Generated Blender identifiers | Use deliberate integer indices for generated suffixes; isolate a native crash in a disposable headless probe before sending it to the GUI. A process abort without a sentinel is an unknown failed outcome. | MANUAL; E1 sentinel | 2026-09-09 float-derived suffix abort |
| Articulated task | Check real joint descendants, pivot frames, critical hand interaction and reachable path; preview story and timing before full render. | Task-specific scripts + MANUAL; E3 | 12:16–12:19 IK/registration failures; 12:13:03 natural-task steering |
| Geometry or motion changes | Invalidate affected topology/contact/load/export evidence by dependency; do not inherit approval from an older hash. | Print parts: ENFORCED by gate hash binding (a report for another scene/spec sha is not reusable). Motion/load evidence: MANUAL; E3/E5 | Wrist-bridge geometry revision; independent flexibility review |
| Surface or media gate passes | Keep media, sampled-contact, fit and manufacturing statuses separate; publish limitations of the actual predicate. | MANUAL; E5 | Current arm independent reviews: sampled screen and manufacture BLOCKED |
| Preview or export verification | Use a disposable process or restore all changed state in `finally`; export re-import must not reset the live working scene. | ENFORCED (E2 done): `preview_render` restores all touched settings in `finally`; `verify_export` runs in a disposable process; tests `tests/execution/test_verify_lib.py` | Preview failure probe leaves samples/resolution/path changed; helper source audit |
| Temporary transform applied for export, measure or plate layout | Restore **every axis you touched** in a `finally`, not only the one you remember; re-read one placed object after the restore and assert it. This row is scoped to build/export/pack passes; `preview_render`/`verify_export` are already ENFORCED above. | MANUAL | 2026-09-06 pass-30: export slid `location.y`, the restore reset only `.x` → parts stayed in a row while the slabs moved |
| Shell command that builds a list, range or file set | zsh: use arrays (`items=(…)`, `"${items[@]}"`) or explicit `${var%…}` expansion; never rely on unquoted `$var` word-splitting. | MANUAL | 2026-09-06: `set -- $rng` and a montage file list, twice in one session |
| Full render | Preserve input identity and raw frame sequence; validate range/completeness; presentation-only changes reuse source frames. Check final encode and final visual evidence. | Task renderer/video verifier + MANUAL; E4 | 12:40:01 no-subtitle correction; 720 raw frames reused |
| Assembly or exploded view | Validate waiting layout and transit separately from final pose; allow named mate contacts only in their declared phase. Check analogous joints. | MANUAL + Arm-specific sweep/acceptance; generic E6 deferred | Owner screenshot 2026-09-05 22:56:06+07; A5 revisions522afd/e9323/11d88f |
| New synthesis or boilerplate | Review actual code and negative controls before promotion; case constants and source labels are not standards or runtime proof. | Workbench hash-bound review; MANUAL technical judgment | 2026-09-05 session retro; new source RES-CAD-ROB-13 and adapter audit |
| First/last camera and render reuse | Include endpoint framing in cheap preview; reuse changed-file frames only with scoped dependency/state equivalence evidence, never filename equality. | MANUAL; A5 hero-prefix comparison is task-specific, E4 generalization open | A5 decoded fingertip crop and 89-frame hero repair,2026-09-05 |
| HQ reference coverage | The coverage command validates only declared receipt bindings. `--launch` guards its own render-only route; direct Blender, MCP and direct headless calls bypass it. State the actual launch route and retain source/settings/media hashes. | ENFORCED within `hq-coverage-gate.py --launch`; otherwise MANUAL | 2026-09-08 coverage gate and 2026-09-09 C05 direct launch |
| Surprising geometry measurement | Validate the checker before changing geometry. For small evaluated meshes far from origin, translate vertices to a local centroid and use float64 scalar arithmetic for signed volume; rerun the corrected checker on the unchanged source. | MANUAL; task-specific audit | 2026-09-09 C05 false negative volumes |

Status 2026-09-06 (audit program `plans/260905-2356-blender-workflow-audit/`): **E1 implemented** (`agent_runtime.py`, `headless-run.sh`, socket client, 43 tests), **E2 implemented** (`agent_verify/` package, 7 tests), **E3 partial** — `production-gate.py` binds reports to scene+spec hashes for printed parts (19 tests); task/flexibility checkers in `builds/` still print status without failing exit. **E4, E5, E6, E7 open.** New binding rule: part production needs `spec.json` before detailing and a passing gate before delivery (`recipes.md#production-contract`). Existing task checks may satisfy a predicate better than a generic helper; record the checker actually run.
